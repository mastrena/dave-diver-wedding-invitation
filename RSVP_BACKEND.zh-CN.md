# 零基础宾客管理后台教程

> 目标：让宾客填写的姓名、人数、住宿、电话和留言真正汇总到一个后台，并能导出 Excel。整个请柬只改一行代码，不需要购买服务器，也不要删除原有登记表。

## 一、先理解“演示模式”和“真实后台”

项目默认是安全的演示模式：登记内容只保存在填写者自己的浏览器中，新人看不到名单。这样适合公开展示，但不适合正式统计。

需要正式使用时，最省心的办法是使用在线表单平台。表单平台负责保存和导出数据，请柬只负责打开填写页面。

## 二、选择表单平台

可使用金数据、腾讯问卷、WPS 表单或其他你信任的平台。界面、免费额度和导出权限可能调整，请以平台当前说明为准。本教程以“金数据”为例，因为创建、发布和导出流程比较直观。

- 金数据：<https://jinshuju.net/>
- [金数据官方发布说明](https://help.jinshuju.net/v4/articles/wxform)
- [金数据官方导出说明](https://help.jinshuju.net/v4/articles/dataexport/)

## 三、创建婚礼宾客登记表

1. 登录表单平台，新建空白表单，标题可写“我们的婚礼赴约登记”。
2. 按下面顺序添加字段：

| 字段 | 推荐类型 | 是否必填 |
| --- | --- | --- |
| 宾客姓名 | 单行文字 | 是 |
| 是否参加 | 单选：参加 / 遗憾缺席 | 是 |
| 出席人数 | 数字或下拉选择 | 参加时填写 |
| 是否需要住宿 | 单选：需要 / 不需要 | 参加时填写 |
| 入住和退房日期 | 日期 | 需要住宿时填写 |
| 联系电话 | 手机或单行文字 | 选填 |
| 给新人的留言 | 多行文字 | 选填 |

3. 在表单说明中写明数据用途，例如：“仅用于统计婚礼出席、住宿和联络安排，婚礼结束后删除。”
4. 发布表单，用自己的手机提交一条测试记录。
5. 回到表单后台，确认能看到这条记录。

## 四、复制正确的公开填写链接

在表单的“发布/分享”页面复制宾客填写链接。它通常以 `https://` 开头。

不要复制浏览器地址栏里的后台编辑链接，也不要把账号、密码、API 密钥或宾客名单放进项目文件。

## 五、请柬只改一行

1. 用文本编辑器打开 `app.js`。
2. 在最上方找到：

```js
const RSVP_FORM_URL = ''
```

3. 把刚才的公开填写链接粘贴到两个单引号中：

```js
const RSVP_FORM_URL = 'https://你的公开表单链接'
```

4. 保存文件并刷新请柬。登记区会自动变成“填写赴约信息”按钮。

请勿删除 `index.html` 里的登记表，也不要整段替换 `app.js`。以后把链接改回空字符串 `''`，即可恢复本地演示模式。

## 六、查看、筛选和导出名单

1. 登录你创建表单时使用的账号。
2. 打开对应表单的“数据/回收/结果”页面。
3. 按“是否参加”“是否需要住宿”等字段筛选。
4. 使用平台的导出功能下载 Excel 或 CSV。
5. 婚礼结束后，先保留一份确有需要的备份，再关闭收集并按承诺删除不再需要的数据。

## 七、正式分享前必须测试

- [ ] 使用手机打开部署后的 HTTPS 请柬，而不是电脑本地文件。
- [ ] 登记区显示“填写赴约信息”，且按钮能打开正确表单。
- [ ] 用一条虚拟信息完成提交，后台能看到记录。
- [ ] 必填项、人数、住宿条件和成功提示正常。
- [ ] 能导出测试记录，并删除这条测试数据。
- [ ] 不同手机重复打开表单时不会看到其他宾客的信息。

## 八、常见问题

### 点击后仍显示演示表单

确认链接粘贴在 `app.js` 的 `RSVP_FORM_URL` 两个引号之间，并且已重新上传修改后的 `app.js`。若网页仍是旧版本，在网址后加 `?v=2` 再打开。

### 打开的是平台后台或要求宾客登录

复制错了链接。回到“发布/分享”页面，复制“公开填写链接”，并检查表单是否允许目标宾客填写。

### 宾客提交了，但后台没有记录

先确认打开的是同一张表单的数据后台，再用无痕窗口亲自提交一条测试记录。默认演示表单的数据只在填写者手机里，不会进入后台。

### 想把第三方表单直接嵌进请柬

不建议新手这样做。部分平台禁止被网页嵌入，微信内也可能出现登录、滚动或白屏问题。外部按钮方式更稳定；只有平台明确提供官方嵌入代码，并且已在微信和手机浏览器测试通过时再使用。

## 九、隐私提醒

只收集筹备确实需要的信息，限制后台查看人员，开启平台提供的访问保护。电话、住宿等属于个人信息，不要把导出的名单上传到公开 GitHub 仓库，也不要在群聊中传播。
